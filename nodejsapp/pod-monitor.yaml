apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: nodejsapp-pod-monitor
  labels:
    app: nodejsapp
spec:
  podMetricsEndpoints:
    - interval: 15s
      path: /metrics
      scheme: http
      port: web
      honorLabels: true
  selector:
    matchLabels:
      app: nodejsapp
